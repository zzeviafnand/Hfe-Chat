import 'package:flutter/material.dart';

class MasterStatuspage extends StatelessWidget {
  const MasterStatuspage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Using GridView of Status")),
    );
  }
}